При выполнении работы найдены и исправлены следующие ошибки:

Файл index.html.
1. Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".
2. Опечатка в заголовке: "Основныые Интрументы" вместо "Основные Инструменты"
3. Опечатка в подзаголовке: "Тетирование" вместо "Тестирование"
4. Лишний закрывающий тег </p> после <p class="subtitle">Тетирование WEB-приложений</p>

Файл cmd.html.
1. Перепутано описание команд: DIR и CD описаны неверно.
 - DIR должен показывать список файлов, а не "Изменить каталог".
 - CD должен менять каталог, а не "Отображение файлов"
2. Перепутаны DATE и TIME: DATE отвечает за дату, TIME за время
3. Отсутствует ссылка для возврата на главную страницу

Файл mac.html.
1. Неверное описание команды mv (должна перемещать/переименовывать, а не создавать папку)
2. Неверное описание команды mkdir (должна создавать папку, а не перемещать)
3. Опечатка в слове "расфшифровывается" у mkdir

Файл git.html
1. Страница содержит описание команд терминала, а не Git
2. Некорректная ссылка возврата на главную - mac.html вместо index.html
3. Опечатка в тексте ссылки: "Вернуся" вместо "Вернуться"
